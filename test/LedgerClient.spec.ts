import * as supertest from 'supertest'
import * as LedgerClient from '../src/LedgerClient'
const config = require('../resources/config-fabric-network.json');

describe('EventGeneratorInput', () => {
  // it('works', () =>
  // //
  // //LedgerClient.ledgerClient.doQuery()
  // )
})
